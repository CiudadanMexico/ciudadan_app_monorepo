'use strict';

/**
 * VACIADO A PROPOSITO: estas rutas se movieron a
 * src/api/ad/routes/02-ad-sesiones.js para que Strapi las monte bajo
 * /api/ads/sesiones/... (pluralName del api `ad`), que es lo que consume
 * el frontend (/gana/ver-anuncios). Los handlers viven en
 * src/api/ad-session/controllers/ad-session.js.
 */

module.exports = { routes: [] };

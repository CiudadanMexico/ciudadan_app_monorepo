'use strict';

/**
 * driver-verifier-candidacy controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::driver-verifier-candidacy.driver-verifier-candidacy');